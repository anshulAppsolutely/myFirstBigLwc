import { LightningElement } from 'lwc';
import SVG_URL from '@salesforce/resourceUrl/Images';

export default class OwlinLogo extends LightningElement {
    poweredBy = SVG_URL + '/owlin/owlinpower.svg';
}